{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="mt-3">
        <button class="btn btn-primary form-control">{{ button_label|default('Registrar') }}</button>
    </div>
{{ form_end(form) }}
